services:
  - type: web
    name: stock-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --timeout 300 --workers 1 --bind 0.0.0.0:10000 --worker-class sync
    plan: free
