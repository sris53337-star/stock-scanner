services:
  - type: web
    name: stock-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    plan: free
```

4. **Ctrl + S** to save, close Notepad

---

## ✅ Final check — your folder should look exactly like this:
```
📁 stock-scanner
   ├── server.py
   ├── requirements.txt
   └── render.yaml